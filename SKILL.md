# 项目语言规范（强制）

## 适用范围
- 项目中的 UI 文案、代码注释、日志输出、提示信息、错误信息。

## 强制要求
1. **必须使用中文**：以上所有内容必须为中文，不允许改成英文。
2. **恢复策略也必须是中文**：即使因为乱码、编码或迁移问题需要恢复内容，也只能恢复为中文版本，不能用英文替代。
3. **新增内容同样遵守**：后续新增的 UI、注释、日志也必须使用中文。

## 变更检查（提交前）
- 检查 UI 文案是否为中文。
- 检查注释是否为中文。
- 检查日志和错误信息是否为中文。
- 若发现英文替换中文，必须改回中文后再提交。
